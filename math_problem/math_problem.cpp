/*
Calcular la media de una muestra.

Versión 1 - Pedimos la cantidad y los datos de una muestra, inicializamos la media, y por último imprimimos un mensaje de salida.

Versión 2 - Creamos una función para pedir los datos de la muestra.

Versión 3 - Calculamos la suma de los datos, y dividimos la suma entre el número de elementos de la muestra (lo que viene siendo la media), imprimimos por pantalla el resultado.

Versión 4 - Si la cantidad de la muestra introducida es menor o igual a 0, saltará un mensaje de error, y lo volverá a pedir.

     */  

